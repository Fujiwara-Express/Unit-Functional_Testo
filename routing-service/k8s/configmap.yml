apiVersion: v1
kind: ConfigMap
metadata:
  name: routing-service-config
  namespace: routing
  labels:
    app: routing-service
data:
  PORT: "8080"
  REDIS_ADDR: "redis-service:6379"
  DELIVERY_SERVICE_URL: "http://delivery-service.delivery.svc.cluster.local:8081"
  # DATABASE_URL is injected via Secret (see secret.yml)
